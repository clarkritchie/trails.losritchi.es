{
  "name": "bend-routes",
  "version": "0.0.24",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "dependencies": {
    "bootstrap3": "^3.3.5",
    "clipboard": "^1.5.15",
    "clipboard-js": "^0.3.1",
    "react": "^15.3.2",
    "react-bootstrap": "^0.32.4",
    "react-dom": "^15.3.2",
    "react-interval": "^2.1.2",
    "react-select": "^1.0.0-rc.2",
    "selector2": "^1.1.7",
    "underscore": "^1.8.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "foo": "set -e && VERSION_TYPE=${1:-patch} npm version ${VERSION_TYPE}",
    "deploy": "set -e && VERSION_TYPE=${1:-patch} npm version $VERSION_TYPE && npm run build && cd build && AWS_PROFILE=default aws s3 sync . s3://trails.losritchi.es",
    "build": "react-scripts build",
    "postbuild": "rimraf build/**/*.map",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
